name: tests

on:
  push:
  pull_request:

jobs:
  tests:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: go to source directory
      run: cd src
    - name: make
      run: make
    - name: test
      run: make test
