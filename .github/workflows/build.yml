name: build

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: go to source directory
      run: cd src
    - name: make
      run: make
    - name: clean
      run: make clean
    
